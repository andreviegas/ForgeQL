void foo() {
    return;
}

struct Motor {
    double speed;
};

enum State {
    Idle,
    Running,
    Stopped
};

/// Documented function
int bar(int x) {
    if (x > 0) {
        return x;
    }
    return 0;
}

int count = 42;
