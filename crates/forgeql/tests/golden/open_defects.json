{
 "suite": "open_defects",
 "description": "Soft pins for known-open defects. Every expect_fail case here asserts the behaviour that is CORRECT per the documentation or the engine's own stated doctrine, and is expected to fail until the defect is fixed — the runner reports it without going red, and a PASS fails the gate so the fixing commit must promote the case (and tighten its assert where the fix settles a shape this case had to leave open). Each expect_fail travels with a control on the same fixture, so corpus drift or an unregistered corpus fails a control loudly instead of masquerading as the expected failure.",
 "cases": [
  {
   "name": "control_the_fixture_holds_five_negative_one_literals",
   "description": "Anchors the two magic-number cases below: the fixture's -1 literals exist and are countable. If this drifts, the cases below are asserting against a fixture that moved, not a fix.",
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE fql_kind = 'number' WHERE name = '-1' IN 'tests/fixtures/enrichment_patterns.cpp'",
   "assert": {
    "row_count": 5
   }
  },
  {
   "name": "a_negative_one_literal_is_never_magic",
   "description": "The enrichment table documents is_magic as true for unexplained constants and lists -1 in the exemptions (not 0, 1, -1, 2, powers of 2, bitmasks). So no -1 literal may carry is_magic = true.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "all five -1 literals in the fixture are stamped is_magic = true against the documented exemption; the likely mechanism is that the exemption compares the literal without absorbing the unary minus, so -1 never matches the -1 exemption; either the enricher or the doc must move, and changing the enricher moves smell-audit counts on every corpus, so the direction is a behaviour call",
   "fql": "FIND symbols WHERE fql_kind = 'number' WHERE name = '-1' WHERE is_magic = 'true' IN 'tests/fixtures/enrichment_patterns.cpp'",
   "assert": {
    "row_count": 0
   }
  },
  {
   "name": "a_zero_literal_is_never_magic",
   "description": "Same exemption list, sharper contradiction: 0 is the first value on it, yet 39 zero literals in the fixture carry is_magic = true. The stamped zeros sit in conditions and returns (if (a > 0), return 0) while initializer zeros (int x = 0) are exempt — so the exemption is context-dependent where the doc says it is value-dependent.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "zeros in condition and return position are stamped magic while initializer zeros are not; the doc promises a value-based exemption (not 0, 1, -1, 2, powers of 2), so either the enricher stops stamping exempt values in those contexts or the doc says which contexts re-arm the exemption",
   "fql": "FIND symbols WHERE fql_kind = 'number' WHERE name = '0' WHERE is_magic = 'true' IN 'tests/fixtures/enrichment_patterns.cpp'",
   "assert": {
    "row_count": 0
   }
  },
  {
   "name": "control_a_true_valued_boolean_field_is_queryable",
   "description": "Anchors the false-value case below: the has_todo field exists, posts, and answers for its true value on this corpus.",
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE fql_kind = 'function' WHERE has_todo = 'true' LIMIT 3",
   "assert": {
    "row_count": 3
   }
  },
  {
   "name": "a_boolean_fields_false_value_is_queryable",
   "description": "The enrichment tables list has_todo, has_escape, has_shadow, is_recursive and friends as boolean fields without saying only one of the two values is addressable. has_doc = 'false' and is_magic = 'false' answer thousands of rows, so the negative case is already a supported query shape — but has_todo = 'false' (and has_escape, has_shadow, is_recursive) answer zero rows on a corpus full of functions without TODOs, indistinguishable from a corpus claim.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "most boolean enrichers post only their true value, so the false predicate matches nothing and the empty result reads as a fact about the corpus; has_doc and is_magic store both values, which makes the gap inconsistent rather than a documented design; the fix is to post the negative for the rest, or to refuse the predicate with a hint naming the restriction — if refusal is chosen, re-aim this assert to error: true",
   "fql": "FIND symbols WHERE fql_kind = 'function' WHERE has_todo = 'false' LIMIT 3",
   "assert": {
    "row_count": 3
   }
  },
  {
   "name": "control_the_lines_clause_target_exists",
   "description": "Anchors the parse case below: the function the multi-step case reads is present and unique in the fixture.",
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE name = 'controlFlowPatterns' IN 'tests/fixtures/enrichment_patterns.cpp'",
   "assert": {
    "row_count": 1
   }
  },
  {
   "name": "the_advertised_lines_clause_parses_on_show_node",
   "description": "SHOW NODE advertises a lines_clause — the parse error for LINES 2-4 itself lists lines_clause as expected — but the range form, the exact syntax the standalone SHOW LINES statement uses, fails to parse at the hyphen. The node-relative suffix '<id>(n-m)' covers the need, but a clause the grammar advertises should parse, or the error should state the shape it does accept.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "SHOW NODE '<id>' LINES A-B is a parse error while SHOW LINES A-B OF '<file>' works, and the error message lists lines_clause among the expected clauses, which is actively misleading; when the grammar accepts the form, promote this case and pin the returned rows",
   "steps": [
    {
     "fql": "FIND symbols WHERE name = 'controlFlowPatterns' IN 'tests/fixtures/enrichment_patterns.cpp'",
     "assert": {
      "row_count": 1
     },
     "capture": {
      "node": "results.0.node_id"
     }
    },
    {
     "fql": "SHOW NODE '${node}' LINES 2-4"
    }
   ]
  },
  {
   "name": "control_a_known_kind_filters_the_outline",
   "description": "Anchors the unknown-kind case below: a kind inside the closed universe filters the same file's outline to its one struct.",
   "use": "forgeql-pub.frozen",
   "fql": "SHOW outline OF 'tests/fixtures/canonical/canonical.rs' WHERE fql_kind = 'struct'",
   "assert": {
    "row_count": 1
   }
  },
  {
   "name": "an_unknown_kind_value_is_refused_not_silently_empty",
   "description": "fql_kind is a closed universe ('impl' is not in it — Rust impl blocks are 'class'), so a typo'd or guessed kind can never match, yet the outline answers header-only silence indistinguishable from 'the file has none of those'. The engine's own refusal doctrine says zero rows is a claim about the corpus and an error is a fact about the query; a value no language can ever produce deserves the error.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "an unknown fql_kind value silently matches nothing on every filtering verb; FIND at least prints a zero total, SHOW outline prints nothing at all; the fix is a parse- or validation-time check against the closed kind enum, refusing with the accepted values the way node_kind and other unanswerable fields are already refused",
   "fql": "SHOW outline OF 'tests/fixtures/canonical/canonical.rs' WHERE fql_kind = 'impl'",
   "assert": {
    "error": true,
    "error_contains": "fql_kind"
   }
  },
  {
   "name": "control_scoped_variables_share_one_name_count",
   "description": "Anchors the case below, and documents the defect's shape: ten let-bindings named session_id in ten different test files all report the identical usages value — the corpus-wide count of the NAME, not references to any binding.",
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE name = 'session_id' WHERE fql_kind = 'variable' IN 'crates/forgeql-core/tests/**'",
   "assert": {
    "row_count": 10,
    "all_same": "usages"
   }
  },
  {
   "name": "a_local_variables_usages_is_not_a_corpus_wide_name_count",
   "description": "For the blast-radius workflow the agent docs recommend (FIND usages, ORDER BY usages DESC), a local variable carrying a global name-frequency is a trap. The engine does not track scoped references, so the honest answers are suppressing the field on locals (what this asserts: the key absent from the row) or a real per-binding count (re-aim this assert when that is built).",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "every local variable row carries the corpus-wide count of its name (236 for session_id on this corpus), meaningless for a scoped binding and indistinguishable from a real reference count; suppress the field on local-scope variables or compute a per-binding count",
   "fql": "FIND symbols WHERE name = 'session_id' WHERE fql_kind = 'variable' IN 'crates/forgeql-core/tests/**'",
   "assert": {
    "pointer": {
     "/results/0/usages": null
    }
   }
  }
,
  {
   "name": "control_a_long_literal_groups_by_file_correctly",
   "description": "Anchors the cases below. GROUP BY file is counted from the index and admits a WHERE built from fql_kind = and name = / LIKE / MATCHES, and it counts the candidates without testing one of them. This is a case where the counted answer happens to come out right; it is NOT a boundary, and must not be read as one. A name tier that answers still only proposes, so agreement here is the corpus being kind, not the path being exact — see the three cases below, one of which is a longer literal that disagrees by one."
   ,
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE name LIKE '%segment%' GROUP BY file ORDER BY count DESC",
   "assert": {
    "row_count": 20,
    "total": 57,
    "rows": [
     { "count": 339, "group_key": "CHANGELOG.md" },
     { "count": 154, "group_key": "crates/forgeql-core/tests/overlay_parity.rs" }
    ]
   }
  },
  {
   "name": "control_the_scan_counts_the_short_literal",
   "description": "The authoritative answer the case below asserts, taken through the scan: adding a predicate the file fast path does not admit (line >= 0, which holds for every row and removes nothing) sends the same grouping to the pipeline, where each row's own name decides. 165 files hold a symbol whose name contains 'ab'; CHANGELOG.md holds 533 of them."
   ,
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE line >= 0 WHERE name LIKE '%ab%' GROUP BY file ORDER BY count DESC",
   "assert": {
    "row_count": 20,
    "total": 165,
    "rows": [
     { "count": 533, "group_key": "CHANGELOG.md" },
     { "count": 97, "group_key": "crates/forgeql-core/tests/overlay_parity.rs" }
    ]
   }
  },
  {
   "name": "a_counted_group_by_file_counts_only_matching_rows",
   "description": "A GROUP BY file counted from the index must return the counts the scan returns — the same contract every other counted grouping is held to. It does not when the WHERE is a name pattern the trigram tier declines to answer.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "fast_group_by_file counts prefilter_global's candidate bitmap per segment and then clears the residual WHERE, so no candidate is ever tested against its own row. This is its loudest shape: a name literal shorter than the trigram width makes the tier return None, prefilter_global skips the predicate, and the candidate set becomes every row -- crates/forgeql/tests/golden.json is reported with 4,344, which is every row it holds, where 23 of them carry a name containing 'ab', and 220 files are reported where 165 hold the name at all. It is not the only shape, and there is no length below which the count is trustworthy: no name tier verifies, so name =, LIKE and MATCHES can all count high (see a_counted_group_by_file_counts_each_row_once and a_counted_group_by_file_counts_an_answered_pattern_once). Only fql_kind = is exact, so narrowing the admitted predicate list is not a fix. The fix is to make the candidate set authoritative before counting it -- intersect it with each segment's canonical rows and test the residual predicate -- which covers every shape; the counts this asserts are the scan's, from the control above",
   "fql": "FIND symbols WHERE name LIKE '%ab%' GROUP BY file ORDER BY count DESC",
   "assert": {
    "row_count": 20,
    "total": 165,
    "rows": [
     { "count": 533, "group_key": "CHANGELOG.md" },
     { "count": 97, "group_key": "crates/forgeql-core/tests/overlay_parity.rs" }
    ]
   }
  }
,
  {
   "name": "control_a_kind_predicate_groups_by_file_exactly",
   "description": "Anchors the case below by isolating which admitted predicate is exact. The kind postings ARE intersected with each segment's canonical row set at overlay build (step5_build_kind_postings, with the comment saying so), so a fql_kind = predicate beside GROUP BY file counts each row once and the counted answer equals the scan's."
   ,
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE fql_kind = 'number' IN 'crates/forgeql-core/src/transforms/diff.rs' GROUP BY file",
   "assert": {
    "row_count": 1,
    "rows": [
     { "count": 215, "group_key": "crates/forgeql-core/src/transforms/diff.rs" }
    ]
   }
  },
  {
   "name": "control_the_scan_counts_each_name_once",
   "description": "The authoritative answer the case below asserts. This file holds 48 rows named '0'; the row listing returns 48 of them, and the scan's grouping counts 48."
   ,
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE line >= 0 WHERE name = '0' IN 'crates/forgeql-core/src/transforms/diff.rs' GROUP BY file",
   "assert": {
    "row_count": 1,
    "rows": [
     { "count": 48, "group_key": "crates/forgeql-core/src/transforms/diff.rs" }
    ]
   }
  },
  {
   "name": "a_counted_group_by_file_counts_each_row_once",
   "description": "The second half of the counted GROUP BY file defect, and the more insidious one: it needs no pattern, only a plain name equality — the shape the eligibility rule most obviously intends to admit.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "step6_build_name_fst merges each segment's raw name postings into the overlay FST without intersecting them with seg_dedup's canonical bitmap, where step5_build_kind_postings and collect_posting_enrichment both do intersect — so a name posting still carries the intra-segment duplicate rows the scan's dedupe pass collapses. fast_group_by_file counts those candidates without verifying them, and this file reports 57 rows named '0' where it holds 48. So name = is NOT an exact tier, and a fix that narrows group_by_file_fast_path_eligible to fql_kind = and name = would leave this case broken; the fix is to make the candidate set authoritative before counting it, by intersecting with each segment's canonical rows and testing the residual predicate, which is what the pattern cases also need. Canonical-intersecting the name postings at overlay build the way step 5 already does would fix this case alone, and that route changes stored index output -- the overlay carries the name FST, and overlay paths are keyed by ENRICH_VER through versioned_provider -- so it must bump ENRICH_VER in storage/columnar/mod.rs in the same commit, or every cached overlay keeps answering the old way and the change looks green while being dead",
   "fql": "FIND symbols WHERE name = '0' IN 'crates/forgeql-core/src/transforms/diff.rs' GROUP BY file",
   "assert": {
    "row_count": 1,
    "rows": [
     { "count": 48, "group_key": "crates/forgeql-core/src/transforms/diff.rs" }
    ]
   }
  }
,
  {
   "name": "control_the_scan_counts_an_answered_pattern",
   "description": "The authoritative answer for the case below: 768 rows of this file carry a name containing 'name'. The literal is four bytes, so the trigram tier answers it rather than declining — this is not the sub-trigram shape."
   ,
   "use": "forgeql-pub.frozen",
   "fql": "FIND symbols WHERE line >= 0 WHERE name LIKE '%name%' IN 'crates/forgeql/tests/golden.json' GROUP BY file",
   "assert": {
    "row_count": 1,
    "rows": [
     { "count": 768, "group_key": "crates/forgeql/tests/golden.json" }
    ]
   }
  },
  {
   "name": "a_counted_group_by_file_counts_an_answered_pattern_once",
   "description": "The case that says there is no safe literal length. A tier that ANSWERS still only proposes: this four-byte pattern is served by the trigram index, and the counted grouping still comes back one row high. Keep this beside the sub-trigram case so neither is read as marking a boundary the other stays inside.",
   "use": "forgeql-pub.frozen",
   "enforcement": "expect_fail",
   "expect_fail_reason": "counted 769 against the scan's and the row listing's 768. The trigram tier answered this literal, so the candidate set is a real narrowing rather than the whole row range -- and it is still a superset, because name_substring_candidates proposes rows and fast_group_by_file counts them with the residual WHERE cleared. That is the whole defect in one line: no name tier verifies, so there is no literal long enough to trust and no admitted-predicate list that can be narrowed into correctness while a name predicate is on it. Only fql_kind = is exact. Fix by making the candidate set authoritative before counting -- intersect with each segment's canonical rows and test the residual predicate",
   "fql": "FIND symbols WHERE name LIKE '%name%' IN 'crates/forgeql/tests/golden.json' GROUP BY file",
   "assert": {
    "row_count": 1,
    "rows": [
     { "count": 768, "group_key": "crates/forgeql/tests/golden.json" }
    ]
   }
  }
 ]
}
